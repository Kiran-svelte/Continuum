/**
 * Self-service attendance (clock in/out, history, regularization) for any role.
 * Reuses the employee attendance experience; APIs scope to the signed-in user.
 */
export { default } from '@/components/pages/employee/attendance-view';
