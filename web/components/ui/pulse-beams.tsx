/**
 * Pulse beams demo component stub.
 *
 * The experimental pulse beams animation has been deferred.
 * This stub satisfies the import contract so the production build
 * does not fail due to a missing module.
 *
 * @module components/ui/pulse-beams
 */

/**
 * Renders a placeholder for the pulse beams demo.
 * Replace with the real implementation when the animation library is integrated.
 *
 * @returns A div with placeholder text.
 */
export function PulseBeamsFirstDemo() {
  return (
    <div className="flex items-center justify-center h-full w-full p-8 text-center text-white/40 text-sm">
      Pulse Beams — Coming Soon
    </div>
  );
}
