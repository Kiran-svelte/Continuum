'use client';

/**
 * Design-system alias for global Cmd+K search.
 * Routes quick nav via portal nav items; API search handled in CommandKSearch.
 */
export { CommandKSearch as CommandPalette } from '@/components/ui/command-k';
