const fs = require('fs');
let content = fs.readFileSync('d:/Continuum/web/app/employee/(main)/profile/page.tsx', 'utf8');

const regex = /<div className="max-w-3xl space-y-6">[\s\S]*?(<div className="flex items-center gap-2 mt-2">)/;
const replacement = \<>\n      <AmbientBackground />\n      <StaggerContainer className="max-w-4xl space-y-6 relative z-10 w-full mx-auto pb-20">\n      <FadeIn>\n      <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">\n        <div>\n          <h1 className="text-3xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-primary to-primary/60 drop-shadow-[0_0_15px_rgba(var(--primary-rgb),0.5)] tracking-tight">My Profile</h1>\n          <p className="text-muted-foreground mt-2 font-medium text-sm">Your employment details and leave summary</p>\n        </div>\n        {!editing && profile && (\n          <TiltCard>\n            <Button className="gap-2 font-bold text-primary hover:bg-primary/20 bg-primary/10 border border-primary/30 shadow-[0_0_15px_rgba(var(--primary-rgb),0.3)] hover:shadow-[0_0_25px_rgba(var(--primary-rgb),0.5)] transition-all duration-300 rounded-xl" onClick={startEditing}>\n              <Pencil className="w-4 h-4" />\n              Edit Profile\n            </Button>\n          </TiltCard>\n        )}\n      </div>\n      </FadeIn>\n\n      {saveSuccess && (\n        <FadeIn>\n          <div className="rounded-xl px-5 py-4 text-sm font-bold bg-green-500/10 text-green-400 border border-green-500/30 shadow-[0_0_15px_rgba(34,197,94,0.15)] flex items-center gap-3 backdrop-blur-sm">\n            <CheckCircle className="w-5 h-5 shrink-0 shadow-[0_0_10px_rgba(34,197,94,0.3)]" />\n            <span className="flex-1">{saveSuccess}</span>\n          </div>\n        </FadeIn>\n      )}\n\n      {loadingProfile ? (\n        <FadeIn>\n          <TiltCard>\n            <div className="py-16 text-center text-sm font-bold text-white/50 bg-white/5 border border-white/10 rounded-2xl animate-pulse glass-panel">Loading profile...</div>\n          </TiltCard>\n        </FadeIn>\n      ) : profile ? (\n        <>\n          {/* ---- Main Profile Card ---- */}\n          <FadeIn>\n          <TiltCard>\n          <Card className="glass-panel border-white/10 shadow-2xl relative overflow-hidden">\n            <div className="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-primary to-purple-500" />\n            <CardContent className="pt-8 pb-6 px-8 relative z-10">\n              <div className="flex flex-col sm:flex-row items-start sm:items-center gap-6">\n                <div className="w-20 h-20 bg-primary/20 border-2 border-primary/40 rounded-full flex items-center justify-center text-3xl font-bold text-primary shadow-[0_0_20px_rgba(var(--primary-rgb),0.3)]\">\n                  {profile.first_name[0]}{profile.last_name[0]}\n                </div>\n                <div>\n                  <h2 className="text-2xl font-bold text-white drop-shadow-md">\n                    {profile.first_name} {profile.last_name}\n                  </h2>\n                  <p className="text-white/60 text-sm font-medium mt-1">\n                    {profile.designation ?? 'No designation set'} &middot; {profile.department ?? 'No department'}\n                  </p>\n                  \;

content = content.replace(regex, replacement);
fs.writeFileSync('d:/Continuum/web/app/employee/(main)/profile/page.tsx', content);
console.log('Replaced successfully');
